{
  "name": "consultancy",
  "private": true,
  "repository": "https://github.com/fazilbinzaid/a-consul",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server ./app -a localhost -p 8080 -c-1"
  },
  "engines": {
    "node": ">=0.10.0"
  }
}
