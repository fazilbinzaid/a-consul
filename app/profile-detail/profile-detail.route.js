'use strict';

app.
  config(function($stateProvider) {

    // 'ngInject';

    $stateProvider.state({
      name: 'profiles.detail',
      url: '/{profileId}',
      component: 'profileDetail'
      resolve:
    });
  });
